function OnSfsButtonLoaded(sender, args) {
/*
    var parent = $('#' + sender.get_id()).parent();
    var clientState = parent.find("[id$='sfsButtonClientState']").val();


    if (clientState != "" && clientState != null) {
        alert('k ,setting');
        sender.set_navigateUrl(clientState);
        sender.set_autoPostBack(false);
    }
    */
}